GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET     /health                           controllers.HomeController.health()

+ nocsrf
POST    /urls                             controllers.UrlController.addUrl()
GET     /urls                             controllers.UrlController.getUrls(pageNo: Option[Int], pageSize: Option[Int])
GET     /urls/:shortCode                  controllers.UrlController.getUrlByShortCode(shortCode: String)
+ nocsrf
DELETE  /urls/:shortCode                  controllers.UrlController.deleteUrlByShortCode(shortCode: String)

GET     /notifications                    controllers.UrlController.getNotifications(pageNo: Option[Int], pageSize: Option[Int])
+ nocsrf
POST    /user                             controllers.UserController.addUser()
GET     /user/:id                         controllers.UserController.getUserById(id: Long)
+ nocsrf
DELETE  /user/:id                         controllers.UserController.deleteUserById(id: Long)

+ nocsrf
POST    /auth/login                       controllers.AuthController.login()
+ nocsrf
POST    /auth/google/login                controllers.AuthController.googleLogin()
+ nocsrf
POST   /auth/refresh                      controllers.AuthController.refreshTokens()
+ nocsrf
POST    /auth/logout                      controllers.AuthController.logoutUser()

GET    /:shortCode                        controllers.UrlController.redirectUrl(shortCode: String)
